using System;
//Class that has a method that takes in two users and returns a double that represents the compatibility score between the two users
public class CompatibilityScore
{
    public double GetCompatibilityScore(User user1, User user2)
    {

    }
}